import styled from 'styled-components';
import {
  baseModule,
} from '../../../styles/themeSelectors';

/** STYLES */
export const CodeWrapperStyled = styled.div`
  margin ${baseModule(2)}px 0;
`;

CodeWrapperStyled.displayName = 'CodeWrapperStyled';
