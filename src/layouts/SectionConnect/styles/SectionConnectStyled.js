import styled from 'styled-components';

import {
  colorBlue,
  colorWhite,
} from '../../../styles/themeSelectors';

/** STYLES */
export const SectionConnectStyled = styled.section`
  background: ${colorBlue};
  color: ${colorWhite};
`;

SectionConnectStyled.displayName = 'SectionConnectStyled';
